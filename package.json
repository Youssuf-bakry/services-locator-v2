{
  "name": "city-services-locator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npm run dev",
    "lint": "echo 'Add ESLint configuration if needed'",
    "check-env": "node scripts/check-env.js"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "vite": "^4.4.5"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "keywords": [
    "city-services",
    "maps",
    "google-places",
    "location-finder"
  ],
  "description": "A web application to find nearby services like pharmacies, restaurants, and stores in your city"
}